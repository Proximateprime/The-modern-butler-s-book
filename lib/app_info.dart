/// App version from pubspec (`0.1.4+13`). Kept local so Settings needs no plugin.
const kAppVersion = '0.1.4';

/// Full pubspec version, including build number — what Settings/About show.
const kAppVersionLabel = '0.1.4+13';

/// Calendar date of the feature freeze. After this, only bugfixes.
const kFeatureFreezeDate = '2026-08-17';

/// User-facing Deterministic Core one-liner (About v1).
const kDeterministicCoreOneLiner =
    'Diagnosis, ranking, and safety stay deterministic on this device. '
    'A language model never decides what is wrong.';
