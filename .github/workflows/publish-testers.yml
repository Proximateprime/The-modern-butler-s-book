# Rebuild hosted tester artifacts with public Supabase URL + anon only.
# Never pass GROQ_API_KEY or a Groq-shaped token into the build.
# Play Store listing / store upload is out.
name: Publish tester web and APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/publish-testers.yml"
      - "tool/assert_packaged_tester_build.sh"
      - "tool/sync_pages_web.sh"
      - "pubspec.yaml"
      - "lib/**"
      - "android/**"
      - "assets/**"
      - "web/**"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

# PRs validate in parallel. All main publishes (push + dispatch) share one group.
concurrency:
  group: ${{ github.event_name == 'pull_request' && format('publish-testers-pr-{0}', github.event.pull_request.number) || 'publish-testers' }}
  cancel-in-progress: true

# Public The Modern Butlers book project. These are not secrets.
# Groq stays on the Edge Function. Do not dart-define GROQ.
env:
  SUPABASE_URL: https://zfwmrqnywhspgzqmkokn.supabase.co
  SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inpmd21ycW55d2hzcGd6cW1rb2tuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQyNTAxNzIsImV4cCI6MjA5OTgyNjE3Mn0.ETQYufqyG8nKum1TzO0VSCtv9QnGs0k0164OuGTRK8U

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Packaged-copy gate (no Groq key)
        run: bash tool/assert_packaged_tester_build.sh

      - name: Java (APK)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Pub get
        run: flutter pub get

      - name: Build Flutter web (public SUPABASE dart-defines only)
        run: |
          set -euo pipefail
          bash tool/assert_packaged_tester_build.sh
          flutter build web --release --base-href "/The-modern-butler-s-book/" \
            --dart-define=SUPABASE_URL="$SUPABASE_URL" \
            --dart-define=SUPABASE_ANON_KEY="$SUPABASE_ANON_KEY"
          touch build/web/.nojekyll
          bash tool/assert_packaged_tester_build.sh --scan-web

      - name: Deploy gh-pages (Flutter web, Jekyll off)
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          force_orphan: true
          enable_jekyll: false
          user_name: github-actions[bot]
          user_email: 41898282+github-actions[bot]@users.noreply.github.com

      - name: Prefer gh-pages as Pages source
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api -X PUT "repos/${GITHUB_REPOSITORY}/pages" \
            -H "Accept: application/vnd.github+json" \
            --input - <<'EOF' || true
          {"source":{"branch":"gh-pages","path":"/"}}
          EOF

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        continue-on-error: true
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

      - name: Deploy Pages (when source is GitHub Actions)
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        continue-on-error: true
        uses: actions/deploy-pages@v4

      - name: Sync Flutter web onto main for legacy Pages (main /)
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: bash tool/sync_pages_web.sh --commit

      - name: Build sideload APK (debug signing, public SUPABASE dart-defines only)
        run: |
          set -euo pipefail
          bash tool/assert_packaged_tester_build.sh
          flutter build apk --release \
            --dart-define=SUPABASE_URL="$SUPABASE_URL" \
            --dart-define=SUPABASE_ANON_KEY="$SUPABASE_ANON_KEY"

      - name: Scan APK then attach to version-tag release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          VERSION="$(awk '/^version:/{print $2; exit}' pubspec.yaml)"
          TAG="v${VERSION}"
          ASSET="modern-butlers-book-${VERSION}.apk"
          cp build/app/outputs/flutter-apk/app-release.apk "$ASSET"
          bash tool/assert_packaged_tester_build.sh --scan-apk "$ASSET"
          if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
            echo "PR build: APK scanned, not attached to the release."
            exit 0
          fi
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            gh release create "$TAG" --title "$TAG" \
              --notes "Tester sideload APK ${VERSION}. Debug-signed. Not Play Store. Calls phrase via public Supabase URL; Groq key stays on the server."
          fi
          gh release upload "$TAG" "$ASSET" --clobber
