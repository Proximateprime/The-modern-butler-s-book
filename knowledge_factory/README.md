# Knowledge Factory (repo root)

Two different trees:

| Path | Role |
|---|---|
| `lib/knowledge_factory/` | **Production** compiled-in packages (imported by the app). |
| `knowledge_factory/prototype/` | **Authoring experiment** — templates only. Not imported. Not assets. No runtime web. |

See [`prototype/README.md`](prototype/README.md).
